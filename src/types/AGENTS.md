# src/types/

## Conventions

- Keep this directory for shared TypeScript types/interfaces.
- Avoid runtime logic here; keep types importable without side effects.
- Prefer exporting types from barrel files only if it stays clear and avoids cycles.
